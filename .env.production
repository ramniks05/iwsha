# Production on Vercel (www.iwshatrust.org) — same-origin /api via api/[...path].js proxy
VITE_API_BASE_URL=/api

# Backend URL for Vercel serverless proxy (set in Vercel dashboard too):
# BACKEND_API_URL=https://hrntechsolutions.com/iwsha-backend/api
